# frozen_string_literal: true

RSpec.describe SlackBlocks do
  it "has a version number" do
    expect(SlackBlocks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
